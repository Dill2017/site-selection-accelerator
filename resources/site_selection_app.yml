resources:
  apps:
    site_selection_app:
      name: "site-selection"
      description: "Brand Site Selection Accelerator — find whitespace opportunities using geospatial embeddings"
      budget_policy_id: "5b62fa02-8671-46d3-96ac-64c1725dc9d9"
      source_code_path: ../packages/app/.build
      resources:
        - name: "sql-warehouse"
          sql_warehouse:
            id: ${var.warehouse_id}
            permission: "CAN_USE"
