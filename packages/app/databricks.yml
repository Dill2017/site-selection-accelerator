bundle:
  name: site-selection

sync:
  include:
    - .build

artifacts:
  default:
    build: apx build

resources:
  apps:
    site-selection-app:
      name: "site-selection"
      description: "site-selection created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
