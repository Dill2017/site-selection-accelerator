bundle:
  name: site-selection

sync:
  include:
    - .build

artifacts:
  default:
    build: apx build

resources:
  apps:
    site-selection-app:
      name: "site-selection-accelerator"
      description: "Brand Site Selection Accelerator"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
